dataset_root: /datasets/OmniEmbodied_Data
dataset_dirs:
  - ${data.dataset_root}/bottle
  - ${data.dataset_root}/dog
  - ${data.dataset_root}/plate
  - ${data.dataset_root}/pour
text_embedding_cache_dir: ${data.dataset_root}/text_embeds_cache

train:
  _target_: rollingwam.datasets.lerobot3.robot_video_dataset.RobotVideoDataset
  dataset_dirs: ${data.dataset_dirs}

  shape_meta:
    images:
      - key: egocentric
        lerobot_key: observation.images.egocentric
        raw_shape: [3, 480, 640]
        shape: [3, 240, 320]
    state:
      - key: default
        lerobot_key: states
        raw_shape: 43
        shape: 43
    action:
      - key: default
        lerobot_key: action
        raw_shape: 78
        shape: 78

  num_frames: 81
  global_sample_stride: 1
  action_video_freq_ratio: 1
  video_size: [224, 320]
  camera_key: null
  concat_multi_camera: null
  val_set_proportion: 0.0
  is_training_set: true
  pretrained_norm_stats: null
  skip_padding_as_possible: false

  processor:
    _target_: rollingwam.datasets.lerobot.processors.rollingwam_processor.RollingWAMProcessor
    shape_meta: ${data.train.shape_meta}
    num_obs_steps: ${data.train.num_frames}
    num_output_cameras: 1
    action_output_dim: 78
    proprio_output_dim: 43
    delta_action_dim_mask: null
    action_state_transforms: null
    use_stepwise_action_norm: false
    norm_default_mode: min/max
    norm_exception_mode: null
    action_state_merger:
      _target_: rollingwam.datasets.lerobot.transforms.action_state_merger.ConcatLeftAlign
    train_transforms:
      - _target_: rollingwam.datasets.lerobot.transforms.image.ToTensor
      - _target_: torchvision.transforms.Resize
        size: [240, 320]
    val_transforms:
      - _target_: rollingwam.datasets.lerobot.transforms.image.ToTensor
      - _target_: torchvision.transforms.Resize
        size: [240, 320]

  text_embedding_cache_dir: ${data.text_embedding_cache_dir}
  context_len: 128
  video_backend: pyav
