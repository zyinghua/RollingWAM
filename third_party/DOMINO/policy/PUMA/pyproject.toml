[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "PUMA"
authors = [
    {name = "Heng Fang", email = "hengfang@hust.edu.cn"},
    {name = "Shangru Li"},
    {name = "Shuhan Wang"},
    {name = "Xuanyang Xi"},
    {name = "Dingkang Liang", email = "dkliang@hust.edu.cn"},
    {name = "Xiang Bai"},

]
description = "PUMA: A dynamics-aware vision-language-action model for generalizable robotic manipulation in dynamic environments."
version = "1.0.1"
readme = "README.md"
requires-python = ">=3.10"
keywords = [
    "dynamic manipulation",
    "vision-language-action",
    "embodied ai",
    "robot learning",
    "spatiotemporal reasoning",
]
license = {file = "LICENSE"}
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Education",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
"Programming Language :: Python :: 3 :: Only",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
]

[project.urls]
Homepage = "https://github.com/H-EmbodVis/DOMINO"
Repository = "https://github.com/H-EmbodVis/DOMINO"

[project.optional-dependencies]
dev = [
    "black>=24.2.0",
    "gpustat",
    "ipython",
    "pre-commit",
    "ruff>=0.2.2",
]
sagemaker = [
    "boto3",
    "sagemaker"
]


[tool.setuptools.packages.find]
where = ["."]
exclude = [
    "tests",         # test directory
    "cache",         # cache directory
    "playground",
    "results",       # result directory
    "checkpoints",   # model checkpoint
    "scripts",       # script directory
    "eval",          # evaluation related directory
    ".vscode",       # VS Code configuration directory
    "*.egg-info",    # egg-info metadata directory
    "**.log",         # log file
    "*.tmp",         # temporary file
    "*.bak",         # backup file
    "**__pycache__**",   # Python cache directory
    "*.pyc",         # compiled Python file
    "*.pyo",          # optimized Python file
    "**bar**",
    "PUMA/config",

]

[tool.setuptools.package-data]
"cogact" = ["py.typed"]

[tool.black]
line-length = 121
target-version = ["py310"]
preview = true

[tool.ruff]
line-length = 121
target-version = "py310"

[tool.ruff.lint]
select = ["A", "B", "E", "F", "I", "RUF", "W"]
ignore = ["F722"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["E402", "F401"]
